server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/follow_relations
    username: mnmr1996development
    password: mnmr1996development
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: false

eureka:
  client:
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka